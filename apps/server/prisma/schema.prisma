generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model Songs {
  id       String @id @default(uuid())
  title    String
  artist   String
  album    String
  duration Int
  imageUrl String
  mp3Url   String?
}
